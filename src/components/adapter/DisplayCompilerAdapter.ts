
//преобразование данных от компилятора в отображение
export const compilerToDispay = (data: any) => {
    let result: any[] | null = null

    //возвращать будет другое, пока неизвестно что
    return result
}

//преобразование данных от интерфейса в данные для компилятора
export const displayToCompiler = (data: any) => {
    let result: any[] | null = null

    return result
}