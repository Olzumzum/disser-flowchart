import {
    ADD_TWO_BLOCKS,
    CONDITIONAL_OPERATOR,
    LOOP_FOR, LOOP_UNTIL,
    LOOP_WHILE
} from "../../../assets/strings/ConstItemsContextBlockMenu";

export const itemsContexMenu = [
     ADD_TWO_BLOCKS,
    CONDITIONAL_OPERATOR,
    LOOP_WHILE,
    LOOP_FOR,
    LOOP_UNTIL
]