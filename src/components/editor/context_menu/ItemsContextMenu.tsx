import {
    ADD_TWO_BLOCKS,
    CONDITIONAL_OPERATOR,
    LOOP_FOR, LOOP_UNTIL,
    LOOP_WHILE
} from "../../../assets/strings/BlockTransformationTypes";

export const itemsContexMenu = [
    ADD_TWO_BLOCKS,
    CONDITIONAL_OPERATOR,
    LOOP_WHILE,
    LOOP_FOR,
    LOOP_UNTIL
]